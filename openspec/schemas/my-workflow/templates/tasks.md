<!-- design.md がある場合は、それを実装可能な最小単位に分解する。各タスクは、担当者が追加の設計判断なしに着手できる粒度にする。順序は依存関係順とし、同時に実施できるものは明記する。各タスクには変更対象、実施内容、完了条件を含める。「実装する」「テストを追加する」だけの曖昧なタスクは禁止。実施済みになったタスクだけを `[x]` にする。 -->

## 1. <準備・基盤>

<!-- 依存関係、設定、データ移行、共通契約など、後続作業の前提を記述する。不要なら削除する。 -->

- [ ] 1.1 `<対象>` に `<具体的な変更>` を行い、`<要件ID / 要件名>` を満たす。完了条件: `<検証可能な結果>`。

## 2. <機能実装>

<!-- 責務境界または利用者フローごとにグループ化する。必要なら各タスク末尾に依存先を記す。設計書の Component Map / Processing Flow / Decision を参照できるようにする。 -->

- [ ] 2.1 `<ファイルまたはモジュール>` で `<入力>` を受け、`<判断・処理>` を行い、`<出力または副作用>` を実現する。対応: `<要件名・設計節>`。完了条件: `<テストまたは確認結果>`。
- [ ] 2.2 `<ファイルまたはモジュール>` に `<失敗・境界条件>` の処理を追加し、`<期待されるエラー結果と状態>` を保証する。対応: `<要件名・設計節>`。完了条件: `<テストまたは確認結果>`。

## 3. <検証・リリース準備>

<!-- 要件トレーサビリティの全行を検証で閉じる。品質確認、移行、監視、文書更新が該当する場合もここに含める。実行するコマンドまたは手動手順と、合格基準を明記する。 -->

- [ ] 3.1 `<テスト対象>` の `<テスト層>` を追加または更新し、`<前提・操作>` に対して `<期待値>` を検証する。対応: `<要件名>`。完了条件: `<テスト名またはコマンド>` が成功する。
- [ ] 3.2 `<移行・監視・文書・手動確認の対象>` を `<具体的な手順>` で更新する。完了条件: `<確認結果またはロールバック可能な状態>`。

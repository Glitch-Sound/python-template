## Context

<!-- 提案の Why は繰り返さず、実装判断に必要な現状だけを記述する。現在の処理経路・責務境界・既存制約、変更対象の API・データ・外部連携・設定、product.md / tech.md / structure.md の規約を書く。事実と仮定を分け、仮定には検証方法または確定期限を添える。 -->

## Goals / Non-Goals

### Goals

<!-- 設計によって達成する技術的な到達点を、検証可能な形で列挙する。 -->

- <技術的な目標>

### Non-Goals

<!-- 今回の設計・実装で扱わない技術的範囲と理由を列挙する。 -->

- <技術的な非目標>

## Requirements Traceability

<!-- proposal.md と各 spec.md の要件を、下記の設計節・タスクへ一対一以上で対応付ける。未対応の要件、または要件の根拠がない設計判断を残してはならない。 -->

| 要件ID | 対応する設計節 | 実装対象 | 実装タスク | 試験ケース | 検証方法 |
| --- | --- | --- | --- | --- | --- |
| REQ-001 | <例: Architecture> | <モジュール・境界> | 2.1 | TC-001 | <テスト種別・観測結果> |

## Architecture and Responsibilities

<!-- 変更後の構成を、実装者が責務の置き場所を判断せずに済む粒度で記述する。図を使う場合も必ず表と文章で同じ情報を説明する。 -->

### Component Map

| コンポーネント / ファイル候補 | 変更種別 | 責務 | 入力 | 出力・副作用 | 依存先 |
| --- | --- | --- | --- | --- | --- |
| `<path または境界名>` | 追加 / 変更 / 削除 | <ここだけが担う責務> | <型・データ・イベント> | <戻り値、永続化、通知など> | <呼び出し先> |

### Processing Flow

<!-- 正常系を番号付きで記述する。各段階で、呼び出し元、処理主体、入力、判定、出力、永続化・外部呼び出しの有無を明記する。分岐は分岐ごとに番号を振る。 -->

1. <開始条件から完了までの処理。どの責務境界が何を判断するか>

### State and Data Design

<!-- 保存データ・キャッシュ・メッセージ・API ペイロードを追加または変更する場合に必須。各項目の名前、型、必須性、生成元、更新者、ライフサイクル、制約を決める。変更がない場合は「変更なし」と根拠を記述する。 -->

| データ / 契約 | 変更 | フィールド・型・必須性 | 制約・既定値 | 生成・更新主体 | 互換性 / 移行 |
| --- | --- | --- | --- | --- | --- |
| <名称> | 追加 / 変更 / 削除 | `<field>: <type> (required)` | <範囲、一意性、形式> | <コンポーネント> | <旧形式の扱い> |

### Interfaces and Error Handling

<!-- 境界を越える API、イベント、CLI、画面操作、外部サービス呼び出しを列挙する。成功・入力不正・認可失敗・競合・依存先失敗ごとに、呼び出し元へ返す結果と状態変更を確定する。「適切にエラー処理する」とは書かない。 -->

| 境界 / 操作 | 呼び出し元 → 呼び出し先 | 入力 | 成功結果 | 失敗条件 → 結果 | 冪等性・再試行・タイムアウト |
| --- | --- | --- | --- | --- | --- |
| <操作名> | <source> → <target> | <値・検証規則> | <応答・状態変化> | <条件> → <エラー/表示/ログ> | <方針または該当なし> |

## Decisions

<!-- 実装の分岐を残さないため、重要な判断を一件ずつ記録する。各決定には採用内容、根拠、却下した代替案、影響範囲、実装上守る不変条件を含める。未決定事項をここに残してはならない。 -->

### Decision: <判断の題名>

- **採用**: <採用する方式・配置・契約を断定的に記述>
- **根拠**: <要件・制約・比較結果>
- **代替案と不採用理由**: <案> — <不採用理由>
- **実装規則 / 不変条件**: <常に満たす条件、禁止事項、順序>
- **影響**: <変更対象、性能・運用・互換性への影響>

## Edge Cases and Failure Modes

<!-- 要件シナリオを補完する実装上の境界条件を列挙する。検知箇所、状態変更の有無、呼び出し元への結果、ログ・監視、復旧方法を固定する。 -->

| 事象 / 境界条件 | 検知する責務 | 処理・状態変更 | 利用者 / 呼び出し元への結果 | 記録・監視 | 復旧 |
| --- | --- | --- | --- | --- | --- |
| <例: 同一要求の重複> | <component> | <処理> | <応答> | <ログ・メトリクス> | <方法> |

## Security, Performance, and Operations

<!-- 該当する観点だけ記述する。対象外なら「対象外」と理由を明記する。認証・認可、機密情報、入力検証、監査、性能上限、レート制限、監視指標、アラート、設定値と既定値、運用手順を、実装・検証可能な数値または条件で定義する。 -->

| 観点 | 設計上の対応 | 判定基準 / 監視方法 |
| --- | --- | --- |
| <security / performance / operations> | <具体的な対応> | <測定値・ログ・手順> |

## Migration and Rollback

<!-- データ、API、設定、外部連携、リリース順序に変更がある場合に必須。前提条件、実施順、互換期間、完了判定、ロールバック条件と手順を明記する。不要な場合は「不要」と根拠を記述する。 -->

1. <移行または「不要」の根拠>

## Test Design

<!-- spec.md の全Scenarioを一つ以上の試験ケースに対応付ける。各実装変更を伴う試験ケースには pytest 実装を必ず指定する。テスト名だけでなく前提・操作・期待値を書く。 -->

| TC ID | 要件ID | Scenario ID | テスト層 | 前提・操作 | 期待値 | pytest 実装 | 自動化 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-001 | REQ-001 | REQ-001-S01 | unit | <Given/When> | <Then> | `tests/test_module.py::test_behavior` | はい |

## Coverage Confirmation

<!-- 全要件のScenario数と対応TC数を記録する。未対応Scenarioを残して実装・アーカイブしてはならない。 -->

| 要件ID | Scenario数 | 対応TC数 | 未対応Scenario | 状態 |
| --- | --- | --- | --- | --- |
| REQ-001 | 1 | 1 | なし | 網羅 |

## Test Exceptions

<!-- 文書のみの変更などでテストコードを作成しない場合だけ記載する。実装変更での手動試験のみは例外にできない。例外がなければ節全体を削除する。 -->

| 対象要件 / Scenario | 対象外理由 | 承認者 | 期限 |
| --- | --- | --- | --- |
| <ID> | <理由> | <役割または氏名> | <日付> |

## Open Questions

<!-- 仕様、採用方式、タスク分解を変えない未確定事項だけを記述する。回答が設計・実装判断に影響するものは未決のままにせず先に解決する。なければ節全体を削除する。 -->

| 質問 | 回答期限 | 所有者 | 未回答でも実装を進められる理由 |
| --- | --- | --- | --- |
| <質問> | <日付・工程> | <役割> | <理由> |
